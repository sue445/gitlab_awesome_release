require 'spec_helper'

describe GitlabAwesomeRelease do
  it 'has a version number' do
    expect(GitlabAwesomeRelease::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
